name: german_learning_app
description: A progression-based German A1 learning application.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  # Firebase for Backend & Real-time UI
  firebase_core: ^2.24.2
  cloud_firestore: ^4.13.6
  # Payments
  flutterwave_standard: ^1.0.8
  uuid: ^4.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
